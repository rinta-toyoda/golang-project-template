openapi: 3.0.3
info:
  title: example.com API
  version: 1.0.0
  description: |
    Internal API for managing users, skills and projects.
servers:
  - url: http://host.docker.internal:8080/api/v1
  - url: http://localhost:8080/api/v1
tags:
  - name: User Login API
    description: CRUD operations on User

components:
  securitySchemes:
    sessionAuth:        # cookie‑based session (maps to your Gin session middleware)
      type: apiKey
      in: cookie
      name: session_id